@model IEnumerable<NetworkMapData.Network>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NetworkAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Netmask)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gateway)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Broadcast)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NetworkAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Netmask)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gateway)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Broadcast)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
